# Use the SECRET key here, not the publishable one — main.py needs to
# bypass RLS to write counts. This file stays local; never commit real values.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-secret-key-here
